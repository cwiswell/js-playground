import React, { Fragment } from 'react';
import { Typography } from '@material-ui/core';

const Pong = (prop) => {
    

    return (
        <Fragment>
            <Typography variant="h2">
                Pong
            </Typography>
        </Fragment>
    );
}

export default Pong;